import { PageHeader } from "@/components/dashboard/page-header";

export default function VisitorAnalyticsPage() {
  return (
    <PageHeader
      title="Visitor Analytics"
      description="Visitor counts, demographics, and trends over time."
    />
  );
}
