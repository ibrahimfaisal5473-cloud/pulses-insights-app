import { PageHeader } from "@/components/dashboard/page-header";

export default function OverviewPage() {
  return (
    <PageHeader
      title="Overview"
      description="Key visitor metrics at a glance — widgets arrive in a later milestone."
    />
  );
}
