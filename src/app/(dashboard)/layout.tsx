import type { ReactNode } from "react";

/**
 * Layout for the protected dashboard route group.
 *
 * Placeholder for now — the sidebar + header app shell is implemented in a
 * later milestone. Route protection (proxy.ts) is also added later.
 */
export default function DashboardLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
